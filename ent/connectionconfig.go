// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"go.authbricks.com/bricks/ent/connectionconfig"
	"go.authbricks.com/bricks/ent/emailpasswordconnection"
	"go.authbricks.com/bricks/ent/service"
)

// ConnectionConfig is the model entity for the ConnectionConfig schema.
type ConnectionConfig struct {
	config
	// ID of the ent.
	ID string `json:"id" hcl:"id"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ConnectionConfigQuery when eager-loading is set.
	Edges                             ConnectionConfigEdges `json:"edges"`
	service_service_connection_config *string
	selectValues                      sql.SelectValues
}

// ConnectionConfigEdges holds the relations/edges for other nodes in the graph.
type ConnectionConfigEdges struct {
	// Service holds the value of the service edge.
	Service *Service `json:"service,omitempty"`
	// OidcConnections holds the value of the oidc_connections edge.
	OidcConnections []*OIDCConnection `json:"oidc_connections,omitempty"`
	// EmailPasswordConnection holds the value of the email_password_connection edge.
	EmailPasswordConnection *EmailPasswordConnection `json:"email_password_connection,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// ServiceOrErr returns the Service value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ConnectionConfigEdges) ServiceOrErr() (*Service, error) {
	if e.Service != nil {
		return e.Service, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: service.Label}
	}
	return nil, &NotLoadedError{edge: "service"}
}

// OidcConnectionsOrErr returns the OidcConnections value or an error if the edge
// was not loaded in eager-loading.
func (e ConnectionConfigEdges) OidcConnectionsOrErr() ([]*OIDCConnection, error) {
	if e.loadedTypes[1] {
		return e.OidcConnections, nil
	}
	return nil, &NotLoadedError{edge: "oidc_connections"}
}

// EmailPasswordConnectionOrErr returns the EmailPasswordConnection value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ConnectionConfigEdges) EmailPasswordConnectionOrErr() (*EmailPasswordConnection, error) {
	if e.EmailPasswordConnection != nil {
		return e.EmailPasswordConnection, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: emailpasswordconnection.Label}
	}
	return nil, &NotLoadedError{edge: "email_password_connection"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ConnectionConfig) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case connectionconfig.FieldID:
			values[i] = new(sql.NullString)
		case connectionconfig.ForeignKeys[0]: // service_service_connection_config
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ConnectionConfig fields.
func (cc *ConnectionConfig) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case connectionconfig.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				cc.ID = value.String
			}
		case connectionconfig.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field service_service_connection_config", values[i])
			} else if value.Valid {
				cc.service_service_connection_config = new(string)
				*cc.service_service_connection_config = value.String
			}
		default:
			cc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ConnectionConfig.
// This includes values selected through modifiers, order, etc.
func (cc *ConnectionConfig) Value(name string) (ent.Value, error) {
	return cc.selectValues.Get(name)
}

// QueryService queries the "service" edge of the ConnectionConfig entity.
func (cc *ConnectionConfig) QueryService() *ServiceQuery {
	return NewConnectionConfigClient(cc.config).QueryService(cc)
}

// QueryOidcConnections queries the "oidc_connections" edge of the ConnectionConfig entity.
func (cc *ConnectionConfig) QueryOidcConnections() *OIDCConnectionQuery {
	return NewConnectionConfigClient(cc.config).QueryOidcConnections(cc)
}

// QueryEmailPasswordConnection queries the "email_password_connection" edge of the ConnectionConfig entity.
func (cc *ConnectionConfig) QueryEmailPasswordConnection() *EmailPasswordConnectionQuery {
	return NewConnectionConfigClient(cc.config).QueryEmailPasswordConnection(cc)
}

// Update returns a builder for updating this ConnectionConfig.
// Note that you need to call ConnectionConfig.Unwrap() before calling this method if this ConnectionConfig
// was returned from a transaction, and the transaction was committed or rolled back.
func (cc *ConnectionConfig) Update() *ConnectionConfigUpdateOne {
	return NewConnectionConfigClient(cc.config).UpdateOne(cc)
}

// Unwrap unwraps the ConnectionConfig entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (cc *ConnectionConfig) Unwrap() *ConnectionConfig {
	_tx, ok := cc.config.driver.(*txDriver)
	if !ok {
		panic("ent: ConnectionConfig is not a transactional entity")
	}
	cc.config.driver = _tx.drv
	return cc
}

// String implements the fmt.Stringer.
func (cc *ConnectionConfig) String() string {
	var builder strings.Builder
	builder.WriteString("ConnectionConfig(")
	builder.WriteString(fmt.Sprintf("id=%v", cc.ID))
	builder.WriteByte(')')
	return builder.String()
}

// ConnectionConfigs is a parsable slice of ConnectionConfig.
type ConnectionConfigs []*ConnectionConfig
