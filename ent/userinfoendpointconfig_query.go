// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"go.authbricks.com/bricks/ent/predicate"
	"go.authbricks.com/bricks/ent/service"
	"go.authbricks.com/bricks/ent/userinfoendpointconfig"
)

// UserInfoEndpointConfigQuery is the builder for querying UserInfoEndpointConfig entities.
type UserInfoEndpointConfigQuery struct {
	config
	ctx         *QueryContext
	order       []userinfoendpointconfig.OrderOption
	inters      []Interceptor
	predicates  []predicate.UserInfoEndpointConfig
	withService *ServiceQuery
	withFKs     bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserInfoEndpointConfigQuery builder.
func (uiecq *UserInfoEndpointConfigQuery) Where(ps ...predicate.UserInfoEndpointConfig) *UserInfoEndpointConfigQuery {
	uiecq.predicates = append(uiecq.predicates, ps...)
	return uiecq
}

// Limit the number of records to be returned by this query.
func (uiecq *UserInfoEndpointConfigQuery) Limit(limit int) *UserInfoEndpointConfigQuery {
	uiecq.ctx.Limit = &limit
	return uiecq
}

// Offset to start from.
func (uiecq *UserInfoEndpointConfigQuery) Offset(offset int) *UserInfoEndpointConfigQuery {
	uiecq.ctx.Offset = &offset
	return uiecq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (uiecq *UserInfoEndpointConfigQuery) Unique(unique bool) *UserInfoEndpointConfigQuery {
	uiecq.ctx.Unique = &unique
	return uiecq
}

// Order specifies how the records should be ordered.
func (uiecq *UserInfoEndpointConfigQuery) Order(o ...userinfoendpointconfig.OrderOption) *UserInfoEndpointConfigQuery {
	uiecq.order = append(uiecq.order, o...)
	return uiecq
}

// QueryService chains the current query on the "service" edge.
func (uiecq *UserInfoEndpointConfigQuery) QueryService() *ServiceQuery {
	query := (&ServiceClient{config: uiecq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := uiecq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := uiecq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userinfoendpointconfig.Table, userinfoendpointconfig.FieldID, selector),
			sqlgraph.To(service.Table, service.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, userinfoendpointconfig.ServiceTable, userinfoendpointconfig.ServiceColumn),
		)
		fromU = sqlgraph.SetNeighbors(uiecq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first UserInfoEndpointConfig entity from the query.
// Returns a *NotFoundError when no UserInfoEndpointConfig was found.
func (uiecq *UserInfoEndpointConfigQuery) First(ctx context.Context) (*UserInfoEndpointConfig, error) {
	nodes, err := uiecq.Limit(1).All(setContextOp(ctx, uiecq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userinfoendpointconfig.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (uiecq *UserInfoEndpointConfigQuery) FirstX(ctx context.Context) *UserInfoEndpointConfig {
	node, err := uiecq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserInfoEndpointConfig ID from the query.
// Returns a *NotFoundError when no UserInfoEndpointConfig ID was found.
func (uiecq *UserInfoEndpointConfigQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = uiecq.Limit(1).IDs(setContextOp(ctx, uiecq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userinfoendpointconfig.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (uiecq *UserInfoEndpointConfigQuery) FirstIDX(ctx context.Context) string {
	id, err := uiecq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserInfoEndpointConfig entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserInfoEndpointConfig entity is found.
// Returns a *NotFoundError when no UserInfoEndpointConfig entities are found.
func (uiecq *UserInfoEndpointConfigQuery) Only(ctx context.Context) (*UserInfoEndpointConfig, error) {
	nodes, err := uiecq.Limit(2).All(setContextOp(ctx, uiecq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userinfoendpointconfig.Label}
	default:
		return nil, &NotSingularError{userinfoendpointconfig.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (uiecq *UserInfoEndpointConfigQuery) OnlyX(ctx context.Context) *UserInfoEndpointConfig {
	node, err := uiecq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserInfoEndpointConfig ID in the query.
// Returns a *NotSingularError when more than one UserInfoEndpointConfig ID is found.
// Returns a *NotFoundError when no entities are found.
func (uiecq *UserInfoEndpointConfigQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = uiecq.Limit(2).IDs(setContextOp(ctx, uiecq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userinfoendpointconfig.Label}
	default:
		err = &NotSingularError{userinfoendpointconfig.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (uiecq *UserInfoEndpointConfigQuery) OnlyIDX(ctx context.Context) string {
	id, err := uiecq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserInfoEndpointConfigs.
func (uiecq *UserInfoEndpointConfigQuery) All(ctx context.Context) ([]*UserInfoEndpointConfig, error) {
	ctx = setContextOp(ctx, uiecq.ctx, "All")
	if err := uiecq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserInfoEndpointConfig, *UserInfoEndpointConfigQuery]()
	return withInterceptors[[]*UserInfoEndpointConfig](ctx, uiecq, qr, uiecq.inters)
}

// AllX is like All, but panics if an error occurs.
func (uiecq *UserInfoEndpointConfigQuery) AllX(ctx context.Context) []*UserInfoEndpointConfig {
	nodes, err := uiecq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserInfoEndpointConfig IDs.
func (uiecq *UserInfoEndpointConfigQuery) IDs(ctx context.Context) (ids []string, err error) {
	if uiecq.ctx.Unique == nil && uiecq.path != nil {
		uiecq.Unique(true)
	}
	ctx = setContextOp(ctx, uiecq.ctx, "IDs")
	if err = uiecq.Select(userinfoendpointconfig.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (uiecq *UserInfoEndpointConfigQuery) IDsX(ctx context.Context) []string {
	ids, err := uiecq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (uiecq *UserInfoEndpointConfigQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, uiecq.ctx, "Count")
	if err := uiecq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, uiecq, querierCount[*UserInfoEndpointConfigQuery](), uiecq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (uiecq *UserInfoEndpointConfigQuery) CountX(ctx context.Context) int {
	count, err := uiecq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (uiecq *UserInfoEndpointConfigQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, uiecq.ctx, "Exist")
	switch _, err := uiecq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (uiecq *UserInfoEndpointConfigQuery) ExistX(ctx context.Context) bool {
	exist, err := uiecq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserInfoEndpointConfigQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (uiecq *UserInfoEndpointConfigQuery) Clone() *UserInfoEndpointConfigQuery {
	if uiecq == nil {
		return nil
	}
	return &UserInfoEndpointConfigQuery{
		config:      uiecq.config,
		ctx:         uiecq.ctx.Clone(),
		order:       append([]userinfoendpointconfig.OrderOption{}, uiecq.order...),
		inters:      append([]Interceptor{}, uiecq.inters...),
		predicates:  append([]predicate.UserInfoEndpointConfig{}, uiecq.predicates...),
		withService: uiecq.withService.Clone(),
		// clone intermediate query.
		sql:  uiecq.sql.Clone(),
		path: uiecq.path,
	}
}

// WithService tells the query-builder to eager-load the nodes that are connected to
// the "service" edge. The optional arguments are used to configure the query builder of the edge.
func (uiecq *UserInfoEndpointConfigQuery) WithService(opts ...func(*ServiceQuery)) *UserInfoEndpointConfigQuery {
	query := (&ServiceClient{config: uiecq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	uiecq.withService = query
	return uiecq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Endpoint string `json:"endpoint" hcl:"endpoint"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserInfoEndpointConfig.Query().
//		GroupBy(userinfoendpointconfig.FieldEndpoint).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (uiecq *UserInfoEndpointConfigQuery) GroupBy(field string, fields ...string) *UserInfoEndpointConfigGroupBy {
	uiecq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserInfoEndpointConfigGroupBy{build: uiecq}
	grbuild.flds = &uiecq.ctx.Fields
	grbuild.label = userinfoendpointconfig.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Endpoint string `json:"endpoint" hcl:"endpoint"`
//	}
//
//	client.UserInfoEndpointConfig.Query().
//		Select(userinfoendpointconfig.FieldEndpoint).
//		Scan(ctx, &v)
func (uiecq *UserInfoEndpointConfigQuery) Select(fields ...string) *UserInfoEndpointConfigSelect {
	uiecq.ctx.Fields = append(uiecq.ctx.Fields, fields...)
	sbuild := &UserInfoEndpointConfigSelect{UserInfoEndpointConfigQuery: uiecq}
	sbuild.label = userinfoendpointconfig.Label
	sbuild.flds, sbuild.scan = &uiecq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserInfoEndpointConfigSelect configured with the given aggregations.
func (uiecq *UserInfoEndpointConfigQuery) Aggregate(fns ...AggregateFunc) *UserInfoEndpointConfigSelect {
	return uiecq.Select().Aggregate(fns...)
}

func (uiecq *UserInfoEndpointConfigQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range uiecq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, uiecq); err != nil {
				return err
			}
		}
	}
	for _, f := range uiecq.ctx.Fields {
		if !userinfoendpointconfig.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if uiecq.path != nil {
		prev, err := uiecq.path(ctx)
		if err != nil {
			return err
		}
		uiecq.sql = prev
	}
	return nil
}

func (uiecq *UserInfoEndpointConfigQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserInfoEndpointConfig, error) {
	var (
		nodes       = []*UserInfoEndpointConfig{}
		withFKs     = uiecq.withFKs
		_spec       = uiecq.querySpec()
		loadedTypes = [1]bool{
			uiecq.withService != nil,
		}
	)
	if uiecq.withService != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, userinfoendpointconfig.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserInfoEndpointConfig).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserInfoEndpointConfig{config: uiecq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, uiecq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := uiecq.withService; query != nil {
		if err := uiecq.loadService(ctx, query, nodes, nil,
			func(n *UserInfoEndpointConfig, e *Service) { n.Edges.Service = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (uiecq *UserInfoEndpointConfigQuery) loadService(ctx context.Context, query *ServiceQuery, nodes []*UserInfoEndpointConfig, init func(*UserInfoEndpointConfig), assign func(*UserInfoEndpointConfig, *Service)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*UserInfoEndpointConfig)
	for i := range nodes {
		if nodes[i].service_service_user_info_endpoint_config == nil {
			continue
		}
		fk := *nodes[i].service_service_user_info_endpoint_config
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(service.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "service_service_user_info_endpoint_config" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (uiecq *UserInfoEndpointConfigQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := uiecq.querySpec()
	_spec.Node.Columns = uiecq.ctx.Fields
	if len(uiecq.ctx.Fields) > 0 {
		_spec.Unique = uiecq.ctx.Unique != nil && *uiecq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, uiecq.driver, _spec)
}

func (uiecq *UserInfoEndpointConfigQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userinfoendpointconfig.Table, userinfoendpointconfig.Columns, sqlgraph.NewFieldSpec(userinfoendpointconfig.FieldID, field.TypeString))
	_spec.From = uiecq.sql
	if unique := uiecq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if uiecq.path != nil {
		_spec.Unique = true
	}
	if fields := uiecq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userinfoendpointconfig.FieldID)
		for i := range fields {
			if fields[i] != userinfoendpointconfig.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := uiecq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := uiecq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := uiecq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := uiecq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (uiecq *UserInfoEndpointConfigQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(uiecq.driver.Dialect())
	t1 := builder.Table(userinfoendpointconfig.Table)
	columns := uiecq.ctx.Fields
	if len(columns) == 0 {
		columns = userinfoendpointconfig.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if uiecq.sql != nil {
		selector = uiecq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if uiecq.ctx.Unique != nil && *uiecq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range uiecq.predicates {
		p(selector)
	}
	for _, p := range uiecq.order {
		p(selector)
	}
	if offset := uiecq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := uiecq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserInfoEndpointConfigGroupBy is the group-by builder for UserInfoEndpointConfig entities.
type UserInfoEndpointConfigGroupBy struct {
	selector
	build *UserInfoEndpointConfigQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (uiecgb *UserInfoEndpointConfigGroupBy) Aggregate(fns ...AggregateFunc) *UserInfoEndpointConfigGroupBy {
	uiecgb.fns = append(uiecgb.fns, fns...)
	return uiecgb
}

// Scan applies the selector query and scans the result into the given value.
func (uiecgb *UserInfoEndpointConfigGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uiecgb.build.ctx, "GroupBy")
	if err := uiecgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserInfoEndpointConfigQuery, *UserInfoEndpointConfigGroupBy](ctx, uiecgb.build, uiecgb, uiecgb.build.inters, v)
}

func (uiecgb *UserInfoEndpointConfigGroupBy) sqlScan(ctx context.Context, root *UserInfoEndpointConfigQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(uiecgb.fns))
	for _, fn := range uiecgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*uiecgb.flds)+len(uiecgb.fns))
		for _, f := range *uiecgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*uiecgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uiecgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserInfoEndpointConfigSelect is the builder for selecting fields of UserInfoEndpointConfig entities.
type UserInfoEndpointConfigSelect struct {
	*UserInfoEndpointConfigQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (uiecs *UserInfoEndpointConfigSelect) Aggregate(fns ...AggregateFunc) *UserInfoEndpointConfigSelect {
	uiecs.fns = append(uiecs.fns, fns...)
	return uiecs
}

// Scan applies the selector query and scans the result into the given value.
func (uiecs *UserInfoEndpointConfigSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uiecs.ctx, "Select")
	if err := uiecs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserInfoEndpointConfigQuery, *UserInfoEndpointConfigSelect](ctx, uiecs.UserInfoEndpointConfigQuery, uiecs, uiecs.inters, v)
}

func (uiecs *UserInfoEndpointConfigSelect) sqlScan(ctx context.Context, root *UserInfoEndpointConfigQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(uiecs.fns))
	for _, fn := range uiecs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*uiecs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uiecs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
