// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"go.authbricks.com/bricks/ent/application"
	"go.authbricks.com/bricks/ent/codegrant"
	"go.authbricks.com/bricks/ent/predicate"
)

// CodeGrantQuery is the builder for querying CodeGrant entities.
type CodeGrantQuery struct {
	config
	ctx             *QueryContext
	order           []codegrant.OrderOption
	inters          []Interceptor
	predicates      []predicate.CodeGrant
	withApplication *ApplicationQuery
	withFKs         bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CodeGrantQuery builder.
func (cgq *CodeGrantQuery) Where(ps ...predicate.CodeGrant) *CodeGrantQuery {
	cgq.predicates = append(cgq.predicates, ps...)
	return cgq
}

// Limit the number of records to be returned by this query.
func (cgq *CodeGrantQuery) Limit(limit int) *CodeGrantQuery {
	cgq.ctx.Limit = &limit
	return cgq
}

// Offset to start from.
func (cgq *CodeGrantQuery) Offset(offset int) *CodeGrantQuery {
	cgq.ctx.Offset = &offset
	return cgq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cgq *CodeGrantQuery) Unique(unique bool) *CodeGrantQuery {
	cgq.ctx.Unique = &unique
	return cgq
}

// Order specifies how the records should be ordered.
func (cgq *CodeGrantQuery) Order(o ...codegrant.OrderOption) *CodeGrantQuery {
	cgq.order = append(cgq.order, o...)
	return cgq
}

// QueryApplication chains the current query on the "application" edge.
func (cgq *CodeGrantQuery) QueryApplication() *ApplicationQuery {
	query := (&ApplicationClient{config: cgq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cgq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cgq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(codegrant.Table, codegrant.FieldID, selector),
			sqlgraph.To(application.Table, application.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, codegrant.ApplicationTable, codegrant.ApplicationColumn),
		)
		fromU = sqlgraph.SetNeighbors(cgq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CodeGrant entity from the query.
// Returns a *NotFoundError when no CodeGrant was found.
func (cgq *CodeGrantQuery) First(ctx context.Context) (*CodeGrant, error) {
	nodes, err := cgq.Limit(1).All(setContextOp(ctx, cgq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{codegrant.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cgq *CodeGrantQuery) FirstX(ctx context.Context) *CodeGrant {
	node, err := cgq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CodeGrant ID from the query.
// Returns a *NotFoundError when no CodeGrant ID was found.
func (cgq *CodeGrantQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = cgq.Limit(1).IDs(setContextOp(ctx, cgq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{codegrant.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cgq *CodeGrantQuery) FirstIDX(ctx context.Context) string {
	id, err := cgq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CodeGrant entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CodeGrant entity is found.
// Returns a *NotFoundError when no CodeGrant entities are found.
func (cgq *CodeGrantQuery) Only(ctx context.Context) (*CodeGrant, error) {
	nodes, err := cgq.Limit(2).All(setContextOp(ctx, cgq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{codegrant.Label}
	default:
		return nil, &NotSingularError{codegrant.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cgq *CodeGrantQuery) OnlyX(ctx context.Context) *CodeGrant {
	node, err := cgq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CodeGrant ID in the query.
// Returns a *NotSingularError when more than one CodeGrant ID is found.
// Returns a *NotFoundError when no entities are found.
func (cgq *CodeGrantQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = cgq.Limit(2).IDs(setContextOp(ctx, cgq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{codegrant.Label}
	default:
		err = &NotSingularError{codegrant.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cgq *CodeGrantQuery) OnlyIDX(ctx context.Context) string {
	id, err := cgq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CodeGrants.
func (cgq *CodeGrantQuery) All(ctx context.Context) ([]*CodeGrant, error) {
	ctx = setContextOp(ctx, cgq.ctx, "All")
	if err := cgq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CodeGrant, *CodeGrantQuery]()
	return withInterceptors[[]*CodeGrant](ctx, cgq, qr, cgq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cgq *CodeGrantQuery) AllX(ctx context.Context) []*CodeGrant {
	nodes, err := cgq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CodeGrant IDs.
func (cgq *CodeGrantQuery) IDs(ctx context.Context) (ids []string, err error) {
	if cgq.ctx.Unique == nil && cgq.path != nil {
		cgq.Unique(true)
	}
	ctx = setContextOp(ctx, cgq.ctx, "IDs")
	if err = cgq.Select(codegrant.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cgq *CodeGrantQuery) IDsX(ctx context.Context) []string {
	ids, err := cgq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cgq *CodeGrantQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cgq.ctx, "Count")
	if err := cgq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cgq, querierCount[*CodeGrantQuery](), cgq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cgq *CodeGrantQuery) CountX(ctx context.Context) int {
	count, err := cgq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cgq *CodeGrantQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cgq.ctx, "Exist")
	switch _, err := cgq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cgq *CodeGrantQuery) ExistX(ctx context.Context) bool {
	exist, err := cgq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CodeGrantQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cgq *CodeGrantQuery) Clone() *CodeGrantQuery {
	if cgq == nil {
		return nil
	}
	return &CodeGrantQuery{
		config:          cgq.config,
		ctx:             cgq.ctx.Clone(),
		order:           append([]codegrant.OrderOption{}, cgq.order...),
		inters:          append([]Interceptor{}, cgq.inters...),
		predicates:      append([]predicate.CodeGrant{}, cgq.predicates...),
		withApplication: cgq.withApplication.Clone(),
		// clone intermediate query.
		sql:  cgq.sql.Clone(),
		path: cgq.path,
	}
}

// WithApplication tells the query-builder to eager-load the nodes that are connected to
// the "application" edge. The optional arguments are used to configure the query builder of the edge.
func (cgq *CodeGrantQuery) WithApplication(opts ...func(*ApplicationQuery)) *CodeGrantQuery {
	query := (&ApplicationClient{config: cgq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cgq.withApplication = query
	return cgq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Scopes []string `json:"scopes"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CodeGrant.Query().
//		GroupBy(codegrant.FieldScopes).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cgq *CodeGrantQuery) GroupBy(field string, fields ...string) *CodeGrantGroupBy {
	cgq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CodeGrantGroupBy{build: cgq}
	grbuild.flds = &cgq.ctx.Fields
	grbuild.label = codegrant.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Scopes []string `json:"scopes"`
//	}
//
//	client.CodeGrant.Query().
//		Select(codegrant.FieldScopes).
//		Scan(ctx, &v)
func (cgq *CodeGrantQuery) Select(fields ...string) *CodeGrantSelect {
	cgq.ctx.Fields = append(cgq.ctx.Fields, fields...)
	sbuild := &CodeGrantSelect{CodeGrantQuery: cgq}
	sbuild.label = codegrant.Label
	sbuild.flds, sbuild.scan = &cgq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CodeGrantSelect configured with the given aggregations.
func (cgq *CodeGrantQuery) Aggregate(fns ...AggregateFunc) *CodeGrantSelect {
	return cgq.Select().Aggregate(fns...)
}

func (cgq *CodeGrantQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cgq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cgq); err != nil {
				return err
			}
		}
	}
	for _, f := range cgq.ctx.Fields {
		if !codegrant.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cgq.path != nil {
		prev, err := cgq.path(ctx)
		if err != nil {
			return err
		}
		cgq.sql = prev
	}
	return nil
}

func (cgq *CodeGrantQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CodeGrant, error) {
	var (
		nodes       = []*CodeGrant{}
		withFKs     = cgq.withFKs
		_spec       = cgq.querySpec()
		loadedTypes = [1]bool{
			cgq.withApplication != nil,
		}
	)
	if cgq.withApplication != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, codegrant.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CodeGrant).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CodeGrant{config: cgq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cgq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := cgq.withApplication; query != nil {
		if err := cgq.loadApplication(ctx, query, nodes, nil,
			func(n *CodeGrant, e *Application) { n.Edges.Application = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (cgq *CodeGrantQuery) loadApplication(ctx context.Context, query *ApplicationQuery, nodes []*CodeGrant, init func(*CodeGrant), assign func(*CodeGrant, *Application)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*CodeGrant)
	for i := range nodes {
		if nodes[i].application_code_grant == nil {
			continue
		}
		fk := *nodes[i].application_code_grant
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(application.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "application_code_grant" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (cgq *CodeGrantQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cgq.querySpec()
	_spec.Node.Columns = cgq.ctx.Fields
	if len(cgq.ctx.Fields) > 0 {
		_spec.Unique = cgq.ctx.Unique != nil && *cgq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cgq.driver, _spec)
}

func (cgq *CodeGrantQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(codegrant.Table, codegrant.Columns, sqlgraph.NewFieldSpec(codegrant.FieldID, field.TypeString))
	_spec.From = cgq.sql
	if unique := cgq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cgq.path != nil {
		_spec.Unique = true
	}
	if fields := cgq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, codegrant.FieldID)
		for i := range fields {
			if fields[i] != codegrant.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cgq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cgq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cgq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cgq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cgq *CodeGrantQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cgq.driver.Dialect())
	t1 := builder.Table(codegrant.Table)
	columns := cgq.ctx.Fields
	if len(columns) == 0 {
		columns = codegrant.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cgq.sql != nil {
		selector = cgq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cgq.ctx.Unique != nil && *cgq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cgq.predicates {
		p(selector)
	}
	for _, p := range cgq.order {
		p(selector)
	}
	if offset := cgq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cgq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CodeGrantGroupBy is the group-by builder for CodeGrant entities.
type CodeGrantGroupBy struct {
	selector
	build *CodeGrantQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cggb *CodeGrantGroupBy) Aggregate(fns ...AggregateFunc) *CodeGrantGroupBy {
	cggb.fns = append(cggb.fns, fns...)
	return cggb
}

// Scan applies the selector query and scans the result into the given value.
func (cggb *CodeGrantGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cggb.build.ctx, "GroupBy")
	if err := cggb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CodeGrantQuery, *CodeGrantGroupBy](ctx, cggb.build, cggb, cggb.build.inters, v)
}

func (cggb *CodeGrantGroupBy) sqlScan(ctx context.Context, root *CodeGrantQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cggb.fns))
	for _, fn := range cggb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cggb.flds)+len(cggb.fns))
		for _, f := range *cggb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cggb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cggb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CodeGrantSelect is the builder for selecting fields of CodeGrant entities.
type CodeGrantSelect struct {
	*CodeGrantQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cgs *CodeGrantSelect) Aggregate(fns ...AggregateFunc) *CodeGrantSelect {
	cgs.fns = append(cgs.fns, fns...)
	return cgs
}

// Scan applies the selector query and scans the result into the given value.
func (cgs *CodeGrantSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cgs.ctx, "Select")
	if err := cgs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CodeGrantQuery, *CodeGrantSelect](ctx, cgs.CodeGrantQuery, cgs, cgs.inters, v)
}

func (cgs *CodeGrantSelect) sqlScan(ctx context.Context, root *CodeGrantQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cgs.fns))
	for _, fn := range cgs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cgs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cgs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
