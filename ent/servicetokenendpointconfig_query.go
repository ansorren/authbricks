// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"go.authbricks.com/bricks/ent/predicate"
	"go.authbricks.com/bricks/ent/service"
	"go.authbricks.com/bricks/ent/servicetokenendpointconfig"
)

// ServiceTokenEndpointConfigQuery is the builder for querying ServiceTokenEndpointConfig entities.
type ServiceTokenEndpointConfigQuery struct {
	config
	ctx         *QueryContext
	order       []servicetokenendpointconfig.OrderOption
	inters      []Interceptor
	predicates  []predicate.ServiceTokenEndpointConfig
	withService *ServiceQuery
	withFKs     bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ServiceTokenEndpointConfigQuery builder.
func (stecq *ServiceTokenEndpointConfigQuery) Where(ps ...predicate.ServiceTokenEndpointConfig) *ServiceTokenEndpointConfigQuery {
	stecq.predicates = append(stecq.predicates, ps...)
	return stecq
}

// Limit the number of records to be returned by this query.
func (stecq *ServiceTokenEndpointConfigQuery) Limit(limit int) *ServiceTokenEndpointConfigQuery {
	stecq.ctx.Limit = &limit
	return stecq
}

// Offset to start from.
func (stecq *ServiceTokenEndpointConfigQuery) Offset(offset int) *ServiceTokenEndpointConfigQuery {
	stecq.ctx.Offset = &offset
	return stecq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (stecq *ServiceTokenEndpointConfigQuery) Unique(unique bool) *ServiceTokenEndpointConfigQuery {
	stecq.ctx.Unique = &unique
	return stecq
}

// Order specifies how the records should be ordered.
func (stecq *ServiceTokenEndpointConfigQuery) Order(o ...servicetokenendpointconfig.OrderOption) *ServiceTokenEndpointConfigQuery {
	stecq.order = append(stecq.order, o...)
	return stecq
}

// QueryService chains the current query on the "service" edge.
func (stecq *ServiceTokenEndpointConfigQuery) QueryService() *ServiceQuery {
	query := (&ServiceClient{config: stecq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := stecq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := stecq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(servicetokenendpointconfig.Table, servicetokenendpointconfig.FieldID, selector),
			sqlgraph.To(service.Table, service.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, servicetokenendpointconfig.ServiceTable, servicetokenendpointconfig.ServiceColumn),
		)
		fromU = sqlgraph.SetNeighbors(stecq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ServiceTokenEndpointConfig entity from the query.
// Returns a *NotFoundError when no ServiceTokenEndpointConfig was found.
func (stecq *ServiceTokenEndpointConfigQuery) First(ctx context.Context) (*ServiceTokenEndpointConfig, error) {
	nodes, err := stecq.Limit(1).All(setContextOp(ctx, stecq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{servicetokenendpointconfig.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (stecq *ServiceTokenEndpointConfigQuery) FirstX(ctx context.Context) *ServiceTokenEndpointConfig {
	node, err := stecq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ServiceTokenEndpointConfig ID from the query.
// Returns a *NotFoundError when no ServiceTokenEndpointConfig ID was found.
func (stecq *ServiceTokenEndpointConfigQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = stecq.Limit(1).IDs(setContextOp(ctx, stecq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{servicetokenendpointconfig.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (stecq *ServiceTokenEndpointConfigQuery) FirstIDX(ctx context.Context) string {
	id, err := stecq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ServiceTokenEndpointConfig entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ServiceTokenEndpointConfig entity is found.
// Returns a *NotFoundError when no ServiceTokenEndpointConfig entities are found.
func (stecq *ServiceTokenEndpointConfigQuery) Only(ctx context.Context) (*ServiceTokenEndpointConfig, error) {
	nodes, err := stecq.Limit(2).All(setContextOp(ctx, stecq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{servicetokenendpointconfig.Label}
	default:
		return nil, &NotSingularError{servicetokenendpointconfig.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (stecq *ServiceTokenEndpointConfigQuery) OnlyX(ctx context.Context) *ServiceTokenEndpointConfig {
	node, err := stecq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ServiceTokenEndpointConfig ID in the query.
// Returns a *NotSingularError when more than one ServiceTokenEndpointConfig ID is found.
// Returns a *NotFoundError when no entities are found.
func (stecq *ServiceTokenEndpointConfigQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = stecq.Limit(2).IDs(setContextOp(ctx, stecq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{servicetokenendpointconfig.Label}
	default:
		err = &NotSingularError{servicetokenendpointconfig.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (stecq *ServiceTokenEndpointConfigQuery) OnlyIDX(ctx context.Context) string {
	id, err := stecq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ServiceTokenEndpointConfigs.
func (stecq *ServiceTokenEndpointConfigQuery) All(ctx context.Context) ([]*ServiceTokenEndpointConfig, error) {
	ctx = setContextOp(ctx, stecq.ctx, "All")
	if err := stecq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ServiceTokenEndpointConfig, *ServiceTokenEndpointConfigQuery]()
	return withInterceptors[[]*ServiceTokenEndpointConfig](ctx, stecq, qr, stecq.inters)
}

// AllX is like All, but panics if an error occurs.
func (stecq *ServiceTokenEndpointConfigQuery) AllX(ctx context.Context) []*ServiceTokenEndpointConfig {
	nodes, err := stecq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ServiceTokenEndpointConfig IDs.
func (stecq *ServiceTokenEndpointConfigQuery) IDs(ctx context.Context) (ids []string, err error) {
	if stecq.ctx.Unique == nil && stecq.path != nil {
		stecq.Unique(true)
	}
	ctx = setContextOp(ctx, stecq.ctx, "IDs")
	if err = stecq.Select(servicetokenendpointconfig.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (stecq *ServiceTokenEndpointConfigQuery) IDsX(ctx context.Context) []string {
	ids, err := stecq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (stecq *ServiceTokenEndpointConfigQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, stecq.ctx, "Count")
	if err := stecq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, stecq, querierCount[*ServiceTokenEndpointConfigQuery](), stecq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (stecq *ServiceTokenEndpointConfigQuery) CountX(ctx context.Context) int {
	count, err := stecq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (stecq *ServiceTokenEndpointConfigQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, stecq.ctx, "Exist")
	switch _, err := stecq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (stecq *ServiceTokenEndpointConfigQuery) ExistX(ctx context.Context) bool {
	exist, err := stecq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ServiceTokenEndpointConfigQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (stecq *ServiceTokenEndpointConfigQuery) Clone() *ServiceTokenEndpointConfigQuery {
	if stecq == nil {
		return nil
	}
	return &ServiceTokenEndpointConfigQuery{
		config:      stecq.config,
		ctx:         stecq.ctx.Clone(),
		order:       append([]servicetokenendpointconfig.OrderOption{}, stecq.order...),
		inters:      append([]Interceptor{}, stecq.inters...),
		predicates:  append([]predicate.ServiceTokenEndpointConfig{}, stecq.predicates...),
		withService: stecq.withService.Clone(),
		// clone intermediate query.
		sql:  stecq.sql.Clone(),
		path: stecq.path,
	}
}

// WithService tells the query-builder to eager-load the nodes that are connected to
// the "service" edge. The optional arguments are used to configure the query builder of the edge.
func (stecq *ServiceTokenEndpointConfigQuery) WithService(opts ...func(*ServiceQuery)) *ServiceTokenEndpointConfigQuery {
	query := (&ServiceClient{config: stecq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	stecq.withService = query
	return stecq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Endpoint string `json:"endpoint" hcl:"endpoint"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ServiceTokenEndpointConfig.Query().
//		GroupBy(servicetokenendpointconfig.FieldEndpoint).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (stecq *ServiceTokenEndpointConfigQuery) GroupBy(field string, fields ...string) *ServiceTokenEndpointConfigGroupBy {
	stecq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ServiceTokenEndpointConfigGroupBy{build: stecq}
	grbuild.flds = &stecq.ctx.Fields
	grbuild.label = servicetokenendpointconfig.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Endpoint string `json:"endpoint" hcl:"endpoint"`
//	}
//
//	client.ServiceTokenEndpointConfig.Query().
//		Select(servicetokenendpointconfig.FieldEndpoint).
//		Scan(ctx, &v)
func (stecq *ServiceTokenEndpointConfigQuery) Select(fields ...string) *ServiceTokenEndpointConfigSelect {
	stecq.ctx.Fields = append(stecq.ctx.Fields, fields...)
	sbuild := &ServiceTokenEndpointConfigSelect{ServiceTokenEndpointConfigQuery: stecq}
	sbuild.label = servicetokenendpointconfig.Label
	sbuild.flds, sbuild.scan = &stecq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ServiceTokenEndpointConfigSelect configured with the given aggregations.
func (stecq *ServiceTokenEndpointConfigQuery) Aggregate(fns ...AggregateFunc) *ServiceTokenEndpointConfigSelect {
	return stecq.Select().Aggregate(fns...)
}

func (stecq *ServiceTokenEndpointConfigQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range stecq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, stecq); err != nil {
				return err
			}
		}
	}
	for _, f := range stecq.ctx.Fields {
		if !servicetokenendpointconfig.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if stecq.path != nil {
		prev, err := stecq.path(ctx)
		if err != nil {
			return err
		}
		stecq.sql = prev
	}
	return nil
}

func (stecq *ServiceTokenEndpointConfigQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ServiceTokenEndpointConfig, error) {
	var (
		nodes       = []*ServiceTokenEndpointConfig{}
		withFKs     = stecq.withFKs
		_spec       = stecq.querySpec()
		loadedTypes = [1]bool{
			stecq.withService != nil,
		}
	)
	if stecq.withService != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, servicetokenendpointconfig.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ServiceTokenEndpointConfig).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ServiceTokenEndpointConfig{config: stecq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, stecq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := stecq.withService; query != nil {
		if err := stecq.loadService(ctx, query, nodes, nil,
			func(n *ServiceTokenEndpointConfig, e *Service) { n.Edges.Service = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (stecq *ServiceTokenEndpointConfigQuery) loadService(ctx context.Context, query *ServiceQuery, nodes []*ServiceTokenEndpointConfig, init func(*ServiceTokenEndpointConfig), assign func(*ServiceTokenEndpointConfig, *Service)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*ServiceTokenEndpointConfig)
	for i := range nodes {
		if nodes[i].service_service_token_endpoint_config == nil {
			continue
		}
		fk := *nodes[i].service_service_token_endpoint_config
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(service.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "service_service_token_endpoint_config" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (stecq *ServiceTokenEndpointConfigQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := stecq.querySpec()
	_spec.Node.Columns = stecq.ctx.Fields
	if len(stecq.ctx.Fields) > 0 {
		_spec.Unique = stecq.ctx.Unique != nil && *stecq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, stecq.driver, _spec)
}

func (stecq *ServiceTokenEndpointConfigQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(servicetokenendpointconfig.Table, servicetokenendpointconfig.Columns, sqlgraph.NewFieldSpec(servicetokenendpointconfig.FieldID, field.TypeString))
	_spec.From = stecq.sql
	if unique := stecq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if stecq.path != nil {
		_spec.Unique = true
	}
	if fields := stecq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, servicetokenendpointconfig.FieldID)
		for i := range fields {
			if fields[i] != servicetokenendpointconfig.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := stecq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := stecq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := stecq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := stecq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (stecq *ServiceTokenEndpointConfigQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(stecq.driver.Dialect())
	t1 := builder.Table(servicetokenendpointconfig.Table)
	columns := stecq.ctx.Fields
	if len(columns) == 0 {
		columns = servicetokenendpointconfig.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if stecq.sql != nil {
		selector = stecq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if stecq.ctx.Unique != nil && *stecq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range stecq.predicates {
		p(selector)
	}
	for _, p := range stecq.order {
		p(selector)
	}
	if offset := stecq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := stecq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ServiceTokenEndpointConfigGroupBy is the group-by builder for ServiceTokenEndpointConfig entities.
type ServiceTokenEndpointConfigGroupBy struct {
	selector
	build *ServiceTokenEndpointConfigQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (stecgb *ServiceTokenEndpointConfigGroupBy) Aggregate(fns ...AggregateFunc) *ServiceTokenEndpointConfigGroupBy {
	stecgb.fns = append(stecgb.fns, fns...)
	return stecgb
}

// Scan applies the selector query and scans the result into the given value.
func (stecgb *ServiceTokenEndpointConfigGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, stecgb.build.ctx, "GroupBy")
	if err := stecgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ServiceTokenEndpointConfigQuery, *ServiceTokenEndpointConfigGroupBy](ctx, stecgb.build, stecgb, stecgb.build.inters, v)
}

func (stecgb *ServiceTokenEndpointConfigGroupBy) sqlScan(ctx context.Context, root *ServiceTokenEndpointConfigQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(stecgb.fns))
	for _, fn := range stecgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*stecgb.flds)+len(stecgb.fns))
		for _, f := range *stecgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*stecgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := stecgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ServiceTokenEndpointConfigSelect is the builder for selecting fields of ServiceTokenEndpointConfig entities.
type ServiceTokenEndpointConfigSelect struct {
	*ServiceTokenEndpointConfigQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (stecs *ServiceTokenEndpointConfigSelect) Aggregate(fns ...AggregateFunc) *ServiceTokenEndpointConfigSelect {
	stecs.fns = append(stecs.fns, fns...)
	return stecs
}

// Scan applies the selector query and scans the result into the given value.
func (stecs *ServiceTokenEndpointConfigSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, stecs.ctx, "Select")
	if err := stecs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ServiceTokenEndpointConfigQuery, *ServiceTokenEndpointConfigSelect](ctx, stecs.ServiceTokenEndpointConfigQuery, stecs, stecs.inters, v)
}

func (stecs *ServiceTokenEndpointConfigSelect) sqlScan(ctx context.Context, root *ServiceTokenEndpointConfigQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(stecs.fns))
	for _, fn := range stecs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*stecs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := stecs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
