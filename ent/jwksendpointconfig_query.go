// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"go.authbricks.com/bricks/ent/jwksendpointconfig"
	"go.authbricks.com/bricks/ent/predicate"
	"go.authbricks.com/bricks/ent/service"
)

// JwksEndpointConfigQuery is the builder for querying JwksEndpointConfig entities.
type JwksEndpointConfigQuery struct {
	config
	ctx         *QueryContext
	order       []jwksendpointconfig.OrderOption
	inters      []Interceptor
	predicates  []predicate.JwksEndpointConfig
	withService *ServiceQuery
	withFKs     bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the JwksEndpointConfigQuery builder.
func (jecq *JwksEndpointConfigQuery) Where(ps ...predicate.JwksEndpointConfig) *JwksEndpointConfigQuery {
	jecq.predicates = append(jecq.predicates, ps...)
	return jecq
}

// Limit the number of records to be returned by this query.
func (jecq *JwksEndpointConfigQuery) Limit(limit int) *JwksEndpointConfigQuery {
	jecq.ctx.Limit = &limit
	return jecq
}

// Offset to start from.
func (jecq *JwksEndpointConfigQuery) Offset(offset int) *JwksEndpointConfigQuery {
	jecq.ctx.Offset = &offset
	return jecq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (jecq *JwksEndpointConfigQuery) Unique(unique bool) *JwksEndpointConfigQuery {
	jecq.ctx.Unique = &unique
	return jecq
}

// Order specifies how the records should be ordered.
func (jecq *JwksEndpointConfigQuery) Order(o ...jwksendpointconfig.OrderOption) *JwksEndpointConfigQuery {
	jecq.order = append(jecq.order, o...)
	return jecq
}

// QueryService chains the current query on the "service" edge.
func (jecq *JwksEndpointConfigQuery) QueryService() *ServiceQuery {
	query := (&ServiceClient{config: jecq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := jecq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := jecq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(jwksendpointconfig.Table, jwksendpointconfig.FieldID, selector),
			sqlgraph.To(service.Table, service.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, jwksendpointconfig.ServiceTable, jwksendpointconfig.ServiceColumn),
		)
		fromU = sqlgraph.SetNeighbors(jecq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first JwksEndpointConfig entity from the query.
// Returns a *NotFoundError when no JwksEndpointConfig was found.
func (jecq *JwksEndpointConfigQuery) First(ctx context.Context) (*JwksEndpointConfig, error) {
	nodes, err := jecq.Limit(1).All(setContextOp(ctx, jecq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{jwksendpointconfig.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (jecq *JwksEndpointConfigQuery) FirstX(ctx context.Context) *JwksEndpointConfig {
	node, err := jecq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first JwksEndpointConfig ID from the query.
// Returns a *NotFoundError when no JwksEndpointConfig ID was found.
func (jecq *JwksEndpointConfigQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = jecq.Limit(1).IDs(setContextOp(ctx, jecq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{jwksendpointconfig.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (jecq *JwksEndpointConfigQuery) FirstIDX(ctx context.Context) string {
	id, err := jecq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single JwksEndpointConfig entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one JwksEndpointConfig entity is found.
// Returns a *NotFoundError when no JwksEndpointConfig entities are found.
func (jecq *JwksEndpointConfigQuery) Only(ctx context.Context) (*JwksEndpointConfig, error) {
	nodes, err := jecq.Limit(2).All(setContextOp(ctx, jecq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{jwksendpointconfig.Label}
	default:
		return nil, &NotSingularError{jwksendpointconfig.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (jecq *JwksEndpointConfigQuery) OnlyX(ctx context.Context) *JwksEndpointConfig {
	node, err := jecq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only JwksEndpointConfig ID in the query.
// Returns a *NotSingularError when more than one JwksEndpointConfig ID is found.
// Returns a *NotFoundError when no entities are found.
func (jecq *JwksEndpointConfigQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = jecq.Limit(2).IDs(setContextOp(ctx, jecq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{jwksendpointconfig.Label}
	default:
		err = &NotSingularError{jwksendpointconfig.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (jecq *JwksEndpointConfigQuery) OnlyIDX(ctx context.Context) string {
	id, err := jecq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of JwksEndpointConfigs.
func (jecq *JwksEndpointConfigQuery) All(ctx context.Context) ([]*JwksEndpointConfig, error) {
	ctx = setContextOp(ctx, jecq.ctx, "All")
	if err := jecq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*JwksEndpointConfig, *JwksEndpointConfigQuery]()
	return withInterceptors[[]*JwksEndpointConfig](ctx, jecq, qr, jecq.inters)
}

// AllX is like All, but panics if an error occurs.
func (jecq *JwksEndpointConfigQuery) AllX(ctx context.Context) []*JwksEndpointConfig {
	nodes, err := jecq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of JwksEndpointConfig IDs.
func (jecq *JwksEndpointConfigQuery) IDs(ctx context.Context) (ids []string, err error) {
	if jecq.ctx.Unique == nil && jecq.path != nil {
		jecq.Unique(true)
	}
	ctx = setContextOp(ctx, jecq.ctx, "IDs")
	if err = jecq.Select(jwksendpointconfig.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (jecq *JwksEndpointConfigQuery) IDsX(ctx context.Context) []string {
	ids, err := jecq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (jecq *JwksEndpointConfigQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, jecq.ctx, "Count")
	if err := jecq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, jecq, querierCount[*JwksEndpointConfigQuery](), jecq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (jecq *JwksEndpointConfigQuery) CountX(ctx context.Context) int {
	count, err := jecq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (jecq *JwksEndpointConfigQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, jecq.ctx, "Exist")
	switch _, err := jecq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (jecq *JwksEndpointConfigQuery) ExistX(ctx context.Context) bool {
	exist, err := jecq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the JwksEndpointConfigQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (jecq *JwksEndpointConfigQuery) Clone() *JwksEndpointConfigQuery {
	if jecq == nil {
		return nil
	}
	return &JwksEndpointConfigQuery{
		config:      jecq.config,
		ctx:         jecq.ctx.Clone(),
		order:       append([]jwksendpointconfig.OrderOption{}, jecq.order...),
		inters:      append([]Interceptor{}, jecq.inters...),
		predicates:  append([]predicate.JwksEndpointConfig{}, jecq.predicates...),
		withService: jecq.withService.Clone(),
		// clone intermediate query.
		sql:  jecq.sql.Clone(),
		path: jecq.path,
	}
}

// WithService tells the query-builder to eager-load the nodes that are connected to
// the "service" edge. The optional arguments are used to configure the query builder of the edge.
func (jecq *JwksEndpointConfigQuery) WithService(opts ...func(*ServiceQuery)) *JwksEndpointConfigQuery {
	query := (&ServiceClient{config: jecq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	jecq.withService = query
	return jecq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Endpoint string `json:"endpoint" hcl:"endpoint"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.JwksEndpointConfig.Query().
//		GroupBy(jwksendpointconfig.FieldEndpoint).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (jecq *JwksEndpointConfigQuery) GroupBy(field string, fields ...string) *JwksEndpointConfigGroupBy {
	jecq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &JwksEndpointConfigGroupBy{build: jecq}
	grbuild.flds = &jecq.ctx.Fields
	grbuild.label = jwksendpointconfig.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Endpoint string `json:"endpoint" hcl:"endpoint"`
//	}
//
//	client.JwksEndpointConfig.Query().
//		Select(jwksendpointconfig.FieldEndpoint).
//		Scan(ctx, &v)
func (jecq *JwksEndpointConfigQuery) Select(fields ...string) *JwksEndpointConfigSelect {
	jecq.ctx.Fields = append(jecq.ctx.Fields, fields...)
	sbuild := &JwksEndpointConfigSelect{JwksEndpointConfigQuery: jecq}
	sbuild.label = jwksendpointconfig.Label
	sbuild.flds, sbuild.scan = &jecq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a JwksEndpointConfigSelect configured with the given aggregations.
func (jecq *JwksEndpointConfigQuery) Aggregate(fns ...AggregateFunc) *JwksEndpointConfigSelect {
	return jecq.Select().Aggregate(fns...)
}

func (jecq *JwksEndpointConfigQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range jecq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, jecq); err != nil {
				return err
			}
		}
	}
	for _, f := range jecq.ctx.Fields {
		if !jwksendpointconfig.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if jecq.path != nil {
		prev, err := jecq.path(ctx)
		if err != nil {
			return err
		}
		jecq.sql = prev
	}
	return nil
}

func (jecq *JwksEndpointConfigQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*JwksEndpointConfig, error) {
	var (
		nodes       = []*JwksEndpointConfig{}
		withFKs     = jecq.withFKs
		_spec       = jecq.querySpec()
		loadedTypes = [1]bool{
			jecq.withService != nil,
		}
	)
	if jecq.withService != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, jwksendpointconfig.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*JwksEndpointConfig).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &JwksEndpointConfig{config: jecq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, jecq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := jecq.withService; query != nil {
		if err := jecq.loadService(ctx, query, nodes, nil,
			func(n *JwksEndpointConfig, e *Service) { n.Edges.Service = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (jecq *JwksEndpointConfigQuery) loadService(ctx context.Context, query *ServiceQuery, nodes []*JwksEndpointConfig, init func(*JwksEndpointConfig), assign func(*JwksEndpointConfig, *Service)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*JwksEndpointConfig)
	for i := range nodes {
		if nodes[i].service_service_jwks_endpoint_config == nil {
			continue
		}
		fk := *nodes[i].service_service_jwks_endpoint_config
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(service.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "service_service_jwks_endpoint_config" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (jecq *JwksEndpointConfigQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := jecq.querySpec()
	_spec.Node.Columns = jecq.ctx.Fields
	if len(jecq.ctx.Fields) > 0 {
		_spec.Unique = jecq.ctx.Unique != nil && *jecq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, jecq.driver, _spec)
}

func (jecq *JwksEndpointConfigQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(jwksendpointconfig.Table, jwksendpointconfig.Columns, sqlgraph.NewFieldSpec(jwksendpointconfig.FieldID, field.TypeString))
	_spec.From = jecq.sql
	if unique := jecq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if jecq.path != nil {
		_spec.Unique = true
	}
	if fields := jecq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, jwksendpointconfig.FieldID)
		for i := range fields {
			if fields[i] != jwksendpointconfig.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := jecq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := jecq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := jecq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := jecq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (jecq *JwksEndpointConfigQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(jecq.driver.Dialect())
	t1 := builder.Table(jwksendpointconfig.Table)
	columns := jecq.ctx.Fields
	if len(columns) == 0 {
		columns = jwksendpointconfig.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if jecq.sql != nil {
		selector = jecq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if jecq.ctx.Unique != nil && *jecq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range jecq.predicates {
		p(selector)
	}
	for _, p := range jecq.order {
		p(selector)
	}
	if offset := jecq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := jecq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// JwksEndpointConfigGroupBy is the group-by builder for JwksEndpointConfig entities.
type JwksEndpointConfigGroupBy struct {
	selector
	build *JwksEndpointConfigQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (jecgb *JwksEndpointConfigGroupBy) Aggregate(fns ...AggregateFunc) *JwksEndpointConfigGroupBy {
	jecgb.fns = append(jecgb.fns, fns...)
	return jecgb
}

// Scan applies the selector query and scans the result into the given value.
func (jecgb *JwksEndpointConfigGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, jecgb.build.ctx, "GroupBy")
	if err := jecgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JwksEndpointConfigQuery, *JwksEndpointConfigGroupBy](ctx, jecgb.build, jecgb, jecgb.build.inters, v)
}

func (jecgb *JwksEndpointConfigGroupBy) sqlScan(ctx context.Context, root *JwksEndpointConfigQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(jecgb.fns))
	for _, fn := range jecgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*jecgb.flds)+len(jecgb.fns))
		for _, f := range *jecgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*jecgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := jecgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// JwksEndpointConfigSelect is the builder for selecting fields of JwksEndpointConfig entities.
type JwksEndpointConfigSelect struct {
	*JwksEndpointConfigQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (jecs *JwksEndpointConfigSelect) Aggregate(fns ...AggregateFunc) *JwksEndpointConfigSelect {
	jecs.fns = append(jecs.fns, fns...)
	return jecs
}

// Scan applies the selector query and scans the result into the given value.
func (jecs *JwksEndpointConfigSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, jecs.ctx, "Select")
	if err := jecs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JwksEndpointConfigQuery, *JwksEndpointConfigSelect](ctx, jecs.JwksEndpointConfigQuery, jecs, jecs.inters, v)
}

func (jecs *JwksEndpointConfigSelect) sqlScan(ctx context.Context, root *JwksEndpointConfigQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(jecs.fns))
	for _, fn := range jecs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*jecs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := jecs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
