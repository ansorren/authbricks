// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"go.authbricks.com/bricks/ent/loginendpointconfig"
	"go.authbricks.com/bricks/ent/predicate"
	"go.authbricks.com/bricks/ent/service"
)

// LoginEndpointConfigQuery is the builder for querying LoginEndpointConfig entities.
type LoginEndpointConfigQuery struct {
	config
	ctx         *QueryContext
	order       []loginendpointconfig.OrderOption
	inters      []Interceptor
	predicates  []predicate.LoginEndpointConfig
	withService *ServiceQuery
	withFKs     bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LoginEndpointConfigQuery builder.
func (lecq *LoginEndpointConfigQuery) Where(ps ...predicate.LoginEndpointConfig) *LoginEndpointConfigQuery {
	lecq.predicates = append(lecq.predicates, ps...)
	return lecq
}

// Limit the number of records to be returned by this query.
func (lecq *LoginEndpointConfigQuery) Limit(limit int) *LoginEndpointConfigQuery {
	lecq.ctx.Limit = &limit
	return lecq
}

// Offset to start from.
func (lecq *LoginEndpointConfigQuery) Offset(offset int) *LoginEndpointConfigQuery {
	lecq.ctx.Offset = &offset
	return lecq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (lecq *LoginEndpointConfigQuery) Unique(unique bool) *LoginEndpointConfigQuery {
	lecq.ctx.Unique = &unique
	return lecq
}

// Order specifies how the records should be ordered.
func (lecq *LoginEndpointConfigQuery) Order(o ...loginendpointconfig.OrderOption) *LoginEndpointConfigQuery {
	lecq.order = append(lecq.order, o...)
	return lecq
}

// QueryService chains the current query on the "service" edge.
func (lecq *LoginEndpointConfigQuery) QueryService() *ServiceQuery {
	query := (&ServiceClient{config: lecq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := lecq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := lecq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(loginendpointconfig.Table, loginendpointconfig.FieldID, selector),
			sqlgraph.To(service.Table, service.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, loginendpointconfig.ServiceTable, loginendpointconfig.ServiceColumn),
		)
		fromU = sqlgraph.SetNeighbors(lecq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LoginEndpointConfig entity from the query.
// Returns a *NotFoundError when no LoginEndpointConfig was found.
func (lecq *LoginEndpointConfigQuery) First(ctx context.Context) (*LoginEndpointConfig, error) {
	nodes, err := lecq.Limit(1).All(setContextOp(ctx, lecq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{loginendpointconfig.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (lecq *LoginEndpointConfigQuery) FirstX(ctx context.Context) *LoginEndpointConfig {
	node, err := lecq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LoginEndpointConfig ID from the query.
// Returns a *NotFoundError when no LoginEndpointConfig ID was found.
func (lecq *LoginEndpointConfigQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = lecq.Limit(1).IDs(setContextOp(ctx, lecq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{loginendpointconfig.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (lecq *LoginEndpointConfigQuery) FirstIDX(ctx context.Context) string {
	id, err := lecq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LoginEndpointConfig entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LoginEndpointConfig entity is found.
// Returns a *NotFoundError when no LoginEndpointConfig entities are found.
func (lecq *LoginEndpointConfigQuery) Only(ctx context.Context) (*LoginEndpointConfig, error) {
	nodes, err := lecq.Limit(2).All(setContextOp(ctx, lecq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{loginendpointconfig.Label}
	default:
		return nil, &NotSingularError{loginendpointconfig.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (lecq *LoginEndpointConfigQuery) OnlyX(ctx context.Context) *LoginEndpointConfig {
	node, err := lecq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LoginEndpointConfig ID in the query.
// Returns a *NotSingularError when more than one LoginEndpointConfig ID is found.
// Returns a *NotFoundError when no entities are found.
func (lecq *LoginEndpointConfigQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = lecq.Limit(2).IDs(setContextOp(ctx, lecq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{loginendpointconfig.Label}
	default:
		err = &NotSingularError{loginendpointconfig.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (lecq *LoginEndpointConfigQuery) OnlyIDX(ctx context.Context) string {
	id, err := lecq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LoginEndpointConfigs.
func (lecq *LoginEndpointConfigQuery) All(ctx context.Context) ([]*LoginEndpointConfig, error) {
	ctx = setContextOp(ctx, lecq.ctx, "All")
	if err := lecq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LoginEndpointConfig, *LoginEndpointConfigQuery]()
	return withInterceptors[[]*LoginEndpointConfig](ctx, lecq, qr, lecq.inters)
}

// AllX is like All, but panics if an error occurs.
func (lecq *LoginEndpointConfigQuery) AllX(ctx context.Context) []*LoginEndpointConfig {
	nodes, err := lecq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LoginEndpointConfig IDs.
func (lecq *LoginEndpointConfigQuery) IDs(ctx context.Context) (ids []string, err error) {
	if lecq.ctx.Unique == nil && lecq.path != nil {
		lecq.Unique(true)
	}
	ctx = setContextOp(ctx, lecq.ctx, "IDs")
	if err = lecq.Select(loginendpointconfig.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (lecq *LoginEndpointConfigQuery) IDsX(ctx context.Context) []string {
	ids, err := lecq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (lecq *LoginEndpointConfigQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, lecq.ctx, "Count")
	if err := lecq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, lecq, querierCount[*LoginEndpointConfigQuery](), lecq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (lecq *LoginEndpointConfigQuery) CountX(ctx context.Context) int {
	count, err := lecq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (lecq *LoginEndpointConfigQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, lecq.ctx, "Exist")
	switch _, err := lecq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (lecq *LoginEndpointConfigQuery) ExistX(ctx context.Context) bool {
	exist, err := lecq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LoginEndpointConfigQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (lecq *LoginEndpointConfigQuery) Clone() *LoginEndpointConfigQuery {
	if lecq == nil {
		return nil
	}
	return &LoginEndpointConfigQuery{
		config:      lecq.config,
		ctx:         lecq.ctx.Clone(),
		order:       append([]loginendpointconfig.OrderOption{}, lecq.order...),
		inters:      append([]Interceptor{}, lecq.inters...),
		predicates:  append([]predicate.LoginEndpointConfig{}, lecq.predicates...),
		withService: lecq.withService.Clone(),
		// clone intermediate query.
		sql:  lecq.sql.Clone(),
		path: lecq.path,
	}
}

// WithService tells the query-builder to eager-load the nodes that are connected to
// the "service" edge. The optional arguments are used to configure the query builder of the edge.
func (lecq *LoginEndpointConfigQuery) WithService(opts ...func(*ServiceQuery)) *LoginEndpointConfigQuery {
	query := (&ServiceClient{config: lecq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	lecq.withService = query
	return lecq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Endpoint string `json:"endpoint" hcl:"endpoint"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LoginEndpointConfig.Query().
//		GroupBy(loginendpointconfig.FieldEndpoint).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (lecq *LoginEndpointConfigQuery) GroupBy(field string, fields ...string) *LoginEndpointConfigGroupBy {
	lecq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LoginEndpointConfigGroupBy{build: lecq}
	grbuild.flds = &lecq.ctx.Fields
	grbuild.label = loginendpointconfig.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Endpoint string `json:"endpoint" hcl:"endpoint"`
//	}
//
//	client.LoginEndpointConfig.Query().
//		Select(loginendpointconfig.FieldEndpoint).
//		Scan(ctx, &v)
func (lecq *LoginEndpointConfigQuery) Select(fields ...string) *LoginEndpointConfigSelect {
	lecq.ctx.Fields = append(lecq.ctx.Fields, fields...)
	sbuild := &LoginEndpointConfigSelect{LoginEndpointConfigQuery: lecq}
	sbuild.label = loginendpointconfig.Label
	sbuild.flds, sbuild.scan = &lecq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LoginEndpointConfigSelect configured with the given aggregations.
func (lecq *LoginEndpointConfigQuery) Aggregate(fns ...AggregateFunc) *LoginEndpointConfigSelect {
	return lecq.Select().Aggregate(fns...)
}

func (lecq *LoginEndpointConfigQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range lecq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, lecq); err != nil {
				return err
			}
		}
	}
	for _, f := range lecq.ctx.Fields {
		if !loginendpointconfig.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if lecq.path != nil {
		prev, err := lecq.path(ctx)
		if err != nil {
			return err
		}
		lecq.sql = prev
	}
	return nil
}

func (lecq *LoginEndpointConfigQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LoginEndpointConfig, error) {
	var (
		nodes       = []*LoginEndpointConfig{}
		withFKs     = lecq.withFKs
		_spec       = lecq.querySpec()
		loadedTypes = [1]bool{
			lecq.withService != nil,
		}
	)
	if lecq.withService != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, loginendpointconfig.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LoginEndpointConfig).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LoginEndpointConfig{config: lecq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, lecq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := lecq.withService; query != nil {
		if err := lecq.loadService(ctx, query, nodes, nil,
			func(n *LoginEndpointConfig, e *Service) { n.Edges.Service = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (lecq *LoginEndpointConfigQuery) loadService(ctx context.Context, query *ServiceQuery, nodes []*LoginEndpointConfig, init func(*LoginEndpointConfig), assign func(*LoginEndpointConfig, *Service)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*LoginEndpointConfig)
	for i := range nodes {
		if nodes[i].service_service_login_endpoint_config == nil {
			continue
		}
		fk := *nodes[i].service_service_login_endpoint_config
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(service.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "service_service_login_endpoint_config" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (lecq *LoginEndpointConfigQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := lecq.querySpec()
	_spec.Node.Columns = lecq.ctx.Fields
	if len(lecq.ctx.Fields) > 0 {
		_spec.Unique = lecq.ctx.Unique != nil && *lecq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, lecq.driver, _spec)
}

func (lecq *LoginEndpointConfigQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(loginendpointconfig.Table, loginendpointconfig.Columns, sqlgraph.NewFieldSpec(loginendpointconfig.FieldID, field.TypeString))
	_spec.From = lecq.sql
	if unique := lecq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if lecq.path != nil {
		_spec.Unique = true
	}
	if fields := lecq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, loginendpointconfig.FieldID)
		for i := range fields {
			if fields[i] != loginendpointconfig.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := lecq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := lecq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := lecq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := lecq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (lecq *LoginEndpointConfigQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(lecq.driver.Dialect())
	t1 := builder.Table(loginendpointconfig.Table)
	columns := lecq.ctx.Fields
	if len(columns) == 0 {
		columns = loginendpointconfig.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if lecq.sql != nil {
		selector = lecq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if lecq.ctx.Unique != nil && *lecq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range lecq.predicates {
		p(selector)
	}
	for _, p := range lecq.order {
		p(selector)
	}
	if offset := lecq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := lecq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LoginEndpointConfigGroupBy is the group-by builder for LoginEndpointConfig entities.
type LoginEndpointConfigGroupBy struct {
	selector
	build *LoginEndpointConfigQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (lecgb *LoginEndpointConfigGroupBy) Aggregate(fns ...AggregateFunc) *LoginEndpointConfigGroupBy {
	lecgb.fns = append(lecgb.fns, fns...)
	return lecgb
}

// Scan applies the selector query and scans the result into the given value.
func (lecgb *LoginEndpointConfigGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lecgb.build.ctx, "GroupBy")
	if err := lecgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LoginEndpointConfigQuery, *LoginEndpointConfigGroupBy](ctx, lecgb.build, lecgb, lecgb.build.inters, v)
}

func (lecgb *LoginEndpointConfigGroupBy) sqlScan(ctx context.Context, root *LoginEndpointConfigQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(lecgb.fns))
	for _, fn := range lecgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*lecgb.flds)+len(lecgb.fns))
		for _, f := range *lecgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*lecgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lecgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LoginEndpointConfigSelect is the builder for selecting fields of LoginEndpointConfig entities.
type LoginEndpointConfigSelect struct {
	*LoginEndpointConfigQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (lecs *LoginEndpointConfigSelect) Aggregate(fns ...AggregateFunc) *LoginEndpointConfigSelect {
	lecs.fns = append(lecs.fns, fns...)
	return lecs
}

// Scan applies the selector query and scans the result into the given value.
func (lecs *LoginEndpointConfigSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lecs.ctx, "Select")
	if err := lecs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LoginEndpointConfigQuery, *LoginEndpointConfigSelect](ctx, lecs.LoginEndpointConfigQuery, lecs, lecs.inters, v)
}

func (lecs *LoginEndpointConfigSelect) sqlScan(ctx context.Context, root *LoginEndpointConfigQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(lecs.fns))
	for _, fn := range lecs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*lecs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lecs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
