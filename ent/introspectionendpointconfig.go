// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"go.authbricks.com/bricks/ent/introspectionendpointconfig"
	"go.authbricks.com/bricks/ent/service"
)

// IntrospectionEndpointConfig is the model entity for the IntrospectionEndpointConfig schema.
type IntrospectionEndpointConfig struct {
	config `hcl:"-" json:"-"`
	// ID of the ent.
	ID string `json:"id" hcl:"id"`
	// Endpoint holds the value of the "endpoint" field.
	Endpoint string `json:"endpoint" hcl:"endpoint"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the IntrospectionEndpointConfigQuery when eager-loading is set.
	Edges                                         IntrospectionEndpointConfigEdges `json:"edges"`
	service_service_introspection_endpoint_config *string
	selectValues                                  sql.SelectValues
}

// IntrospectionEndpointConfigEdges holds the relations/edges for other nodes in the graph.
type IntrospectionEndpointConfigEdges struct {
	// Service holds the value of the service edge.
	Service *Service `json:"service,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ServiceOrErr returns the Service value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e IntrospectionEndpointConfigEdges) ServiceOrErr() (*Service, error) {
	if e.Service != nil {
		return e.Service, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: service.Label}
	}
	return nil, &NotLoadedError{edge: "service"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*IntrospectionEndpointConfig) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case introspectionendpointconfig.FieldID, introspectionendpointconfig.FieldEndpoint:
			values[i] = new(sql.NullString)
		case introspectionendpointconfig.ForeignKeys[0]: // service_service_introspection_endpoint_config
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the IntrospectionEndpointConfig fields.
func (iec *IntrospectionEndpointConfig) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case introspectionendpointconfig.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				iec.ID = value.String
			}
		case introspectionendpointconfig.FieldEndpoint:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field endpoint", values[i])
			} else if value.Valid {
				iec.Endpoint = value.String
			}
		case introspectionendpointconfig.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field service_service_introspection_endpoint_config", values[i])
			} else if value.Valid {
				iec.service_service_introspection_endpoint_config = new(string)
				*iec.service_service_introspection_endpoint_config = value.String
			}
		default:
			iec.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the IntrospectionEndpointConfig.
// This includes values selected through modifiers, order, etc.
func (iec *IntrospectionEndpointConfig) Value(name string) (ent.Value, error) {
	return iec.selectValues.Get(name)
}

// QueryService queries the "service" edge of the IntrospectionEndpointConfig entity.
func (iec *IntrospectionEndpointConfig) QueryService() *ServiceQuery {
	return NewIntrospectionEndpointConfigClient(iec.config).QueryService(iec)
}

// Update returns a builder for updating this IntrospectionEndpointConfig.
// Note that you need to call IntrospectionEndpointConfig.Unwrap() before calling this method if this IntrospectionEndpointConfig
// was returned from a transaction, and the transaction was committed or rolled back.
func (iec *IntrospectionEndpointConfig) Update() *IntrospectionEndpointConfigUpdateOne {
	return NewIntrospectionEndpointConfigClient(iec.config).UpdateOne(iec)
}

// Unwrap unwraps the IntrospectionEndpointConfig entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (iec *IntrospectionEndpointConfig) Unwrap() *IntrospectionEndpointConfig {
	_tx, ok := iec.config.driver.(*txDriver)
	if !ok {
		panic("ent: IntrospectionEndpointConfig is not a transactional entity")
	}
	iec.config.driver = _tx.drv
	return iec
}

// String implements the fmt.Stringer.
func (iec *IntrospectionEndpointConfig) String() string {
	var builder strings.Builder
	builder.WriteString("IntrospectionEndpointConfig(")
	builder.WriteString(fmt.Sprintf("id=%v, ", iec.ID))
	builder.WriteString("endpoint=")
	builder.WriteString(iec.Endpoint)
	builder.WriteByte(')')
	return builder.String()
}

// IntrospectionEndpointConfigs is a parsable slice of IntrospectionEndpointConfig.
type IntrospectionEndpointConfigs []*IntrospectionEndpointConfig
